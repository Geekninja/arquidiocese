.card
  .card-header.ch-alt
    %h2
      Categorias de Páginas
      = link_to 'Nova Categoria', new_cms_page_category_path, class: 'btn btn-success pull-right'
  .table-responsive{:style => "overflow: hidden; outline: none;", :tabindex => "3"}
    %table#data-table-command.table.table-striped.table-vmiddle.bootgrid-table
      %thead
        %tr
          %th #
          %th Name
          %th Status
          %th{:colspan => "3"}

      %tbody
        - @cms_page_categories.each_with_index do |cms_page_category, i|
          %tr
            %td= i+1
            %td= cms_page_category.name
            %td= status_helper (cms_page_category.status)
            %td.col-md-2
              = link_to '', edit_cms_page_category_path(cms_page_category), class: 'md md-edit btn btn-primary'
              = link_to '', cms_page_category, class: 'md md-delete btn btn-danger', :method => :delete, :data => { :confirm => 'Are you sure?' }
